#include <stdio.h>
#include <math.h>
int a, i=0
float avg:
int main() {
	int num1, num2, sto[a], num_set;
	scanf('%d %d' & (num1, num2));
	a = abs(num1 - num2)
	if (num1 > num2) {
		num_set = num2
		while (num_set < num1) {
			printf('%d' & num_set);
			sto[i] = num_set;
			num_set++;
			i++;
		}
	}
	else {
		num_set = num1
		while (num_set < num2) {
			printf('%d' & num_set);
			sto[i] = num_set;
			num_set++;
			i++;
		}
	}
	avg += for each (int num in sto)
	printf('%.2f' &avg)
}